.cpcache/
.nbb/
node_modules/
out/
target/
package-lock.json

# Minted identities are artifacts for testdata/verify_minted.go to judge, not
# fixtures. CI mints a fresh one every run; a checked-in identity would only
# ever prove that one identity was well formed.
testdata/minted*.edn
